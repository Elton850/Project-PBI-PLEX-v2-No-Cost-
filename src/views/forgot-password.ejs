<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <link rel="stylesheet" href="/public/styles.css"/>
  <title>Reset de senha</title>
</head>
<body class="center">
  <div class="card" style="width:420px;">
    <div style="font-weight:800;">Reset de senha</div>
    <div class="muted" style="margin-bottom:10px;">
      EFETIVO: Email + CPF. &nbsp;|&nbsp; PJ: Email + Código fornecido pelo admin.
    </div>

    <% if (error) { %>
      <div class="error" style="margin-bottom:10px;"><%= error %></div>
    <% } %>

    <form method="post" action="/forgot-password" style="display:flex; flex-direction:column; gap:10px;">
      <div>
        <label>Email</label>
        <input name="email" type="email" required />
      </div>

      <div>
        <label>CPF (somente EFETIVO)</label>
        <input name="cpf" placeholder="Somente números (11 dígitos)" />
      </div>

      <div>
        <label>Código PJ (somente PJ)</label>
        <input name="code" placeholder="Código fornecido pelo admin" />
      </div>

      <button class="btn-primary" type="submit">Continuar</button>

      <div style="text-align:center; margin-top:6px;">
        <a class="chip" href="/login">Voltar</a>
      </div>
    </form>

    <div class="muted" style="margin-top:10px;">
      Se você for PJ e não tiver um código, solicite ao administrador.
    </div>
  </div>
</body>
</html>