<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <link rel="stylesheet" href="/public/styles.css"/>
  <title>Login - Portal BI</title>
</head>
<body class="center">
  <div class="card" style="width:420px;">
    <div style="display:flex; align-items:center; gap:12px; margin-bottom:10px;">
      <div class="logo" style="width:44px;height:44px;border-radius:12px;">
        <img src="/public/img/logo-placeholder.svg" alt="Benel" style="width:100%;height:100%;object-fit:contain;" />
      </div>
      <div>
        <div style="font-weight:800; letter-spacing:.2px;">Portal BI</div>
        <div class="muted">Acesso corporativo • Benel</div>
      </div>
    </div>

    <% if (error) { %>
      <div class="error" style="margin-bottom:10px;"><%= error %></div>
    <% } %>

    <form method="post" action="/login" style="display:flex; flex-direction:column; gap:10px;">
      <div>
        <label>Email</label>
        <input name="email" type="email" required autocomplete="username" />
      </div>

      <div>
        <label>Senha</label>
        <input name="password" type="password" required autocomplete="current-password" />
      </div>

      <button class="btn-primary" type="submit">Entrar</button>

      <div class="muted" style="text-align:center;margin-top:4px;">
        Problemas de acesso? Fale com o administrador.
      </div>
    </form>
  </div>
</body>
</html>